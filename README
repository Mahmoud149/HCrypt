UPDATE MAY 1
------------
way faster again, so that total.py is actually usable. specify plaintext, ciphertext & ciphertext 2 paths like:
<pre><code>python total.py plaintext.txt ciphertext.txt to-decode.txt
</code></pre>
and it will do its thing.

